# Local stub for the private emergent.sh package. Provides just enough surface
# for digest_agents.py to import cleanly. AI digest generation will not work,
# but server.py can boot and the LitPortal integration paths (auth, save,
# handoff) all function without it.
